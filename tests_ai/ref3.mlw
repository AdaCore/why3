
module Test
	use import array.Array
	use import ref.Ref
	use import ref.Refint
	use import int.Int

	let b(x:int): unit
	=
		let k = ref 0 in
		while (!k < x) do
		variant { x - !k }
		invariant { 0 <= !k < x}
		incr k;
		()
		done; ()
end
