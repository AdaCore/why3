module While1

	use import int.Int
	use import ref.Ref

	let b(x:int) : int
	ensures { result = 18 }
	=
		let i = ref 1 in
		let j = ref 0 in
		while !i < 10 do
			variant { 10 - !i  }
			i := !i + 1;
			j := !j + 2;
		done;
		!j
end
