

module Uf1
	use import array.Array
	use import ref.Ref
	use import ref.Refint
	use import int.Int

	let b(x:int): int
	ensures { result = 0 } =
		let a = make 12 3 in
		let k = ref 0 in
		while (!k < 11 && !k >= 0)
		do 
		variant { 12 - !k }
		a[!k+1] <- 0;
		incr k;
		done;

		a[!k]
end
