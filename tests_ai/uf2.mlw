
module Uf1
	use import array.Array
	use import ref.Ref
	use import int.Int

	let b(x:int): int
	ensures { result = 1 } =
		let a = make 12 0 in
		a[0] <- 0;
		a[6] <- 1;
		let i = ref 0 in
	while (!i <= 1) do
	variant { 2 - !i }
	i := !i + 1;
	done; a[6]
end
