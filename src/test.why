
(* test file *)

theory A
  type 'a list
  logic nil : 'a list
  logic cons('a, 'a list) : 'a list

  type t
  logic p('a list)
  logic is_nil(x: 'a list) = p(x)
  logic c : t list
end

theory B
  use import A
  axiom Ax : forall x : t list. true
end

