module M
  use import int.Int
  use import module ref.Ref
  let test1 (x: ref int) =
    { !x >= 0}
    while !x > 0 do
      invariant { !x >= old !x }
      x := !x - 1
    done
    { !x >= old !x }
end

(*
Local Variables:
compile-command: "unset LANG; make -C ../../.. bench/programs/bad-typing/old1"
End:
*)
