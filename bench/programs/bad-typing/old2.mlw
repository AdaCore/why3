module M
  use import int.Int
  use import module ref.Ref
  let test1 (x: ref int) =
    { !x >= 0}
    x := !x - 1;
    assert { !x >= old !x }
    { !x >= old !x }
end

(*
Local Variables:
compile-command: "unset LANG; make -C ../../.. bench/programs/bad-typing/old2"
End:
*)
