
(* theory T *)
(*   type t *)
(* end *)

module P

  use import int.Int
  use import module stdlib.Array

  let foo (t : array int) = 
    { A.length t >= 1 && t[0] = 1 }
    t[0 <- 1];
    t[0] + 3
    { result = 3 }

end

(*
Local Variables: 
compile-command: "unset LANG; make -C .. testl-ide"
End: 
*)
